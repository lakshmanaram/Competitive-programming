#include <bits/stdc++.h>
#define ll long long
#define pb push_back
#define mp make_pair
#define vll vector<long long> 
#define S(a) scanf("%lld", &a)
using namespace std;
int main(){
	long n;
	cin>>n;
	vector<ll> arr(n);
	for(long i=0;i<n;i++){
		cin>>arr[i];
	}
	long m;
	S(m);
	while(m--) {
		long l, r;
		
		S(l);
		S(r);
	}
	return 0;
}